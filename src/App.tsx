import { BrowserRouter } from 'react-router-dom';
import RoutesApp from './routes/RoutesApp';

const App = () => {
  return (
    <BrowserRouter>
      <RoutesApp />
    </BrowserRouter>
  );
};

export default App;
